import { Card, CardContent, Typography } from "@mui/material";

const Dashboard = () => {
    <Card>
        <CardContent>
            <Typography variant="h5">ADMIN DASHBOARD</Typography>
            <Typography>관리 항목 요약 정보는 여기에 표시됩니다.</Typography>
        </CardContent>
    </Card>
}

export default Dashboard;
